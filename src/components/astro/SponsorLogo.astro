---
interface Props {
  src: string;
  href: string;
  alt?: string;
}

const { src, href, alt } = Astro.props;
---

<a href={href} target='_blank'>
  <img src={src} alt={alt} class='w-56 rounded-xl' />
</a>
