---
interface Props {
  src: string;
  href: string;
  alt?: string;
}

const { src, href, alt } = Astro.props;
---

<a href={href} target='_blank'>
  <img src={src} alt={alt} class='rounded-xl w-56' />
</a>
